---
layout: base-page

title: Home

# opt out of collections
eleventyExcludeFromCollections: true

---

<section class="metadata">
</section>

<section class="content">

<p>Hey, thanks for stopping by! There's not much here at the moment, because I'm redoing everything.</p>

<h3>Last 10 Posts</h3>
<p>TBD</p>

{# TODO: add this when I have more posts #}
{# <h3>Favorite</h3> #}
{# <i>Start here if you're new</i> #}
{# <p>TBD</p> #}

<h3>Recent Updates</h3>
<p><i>Because I tend to work incrementally, writing and editing over time</i></p>

{# select only things that are in-progress (TODO: is that right?) #}
{# TODO: limit this (to ten?) #}
<ul>
{%- for post in collections.all | selectattr("data", "inProgress") | sort(true, true, 'data.lastEditDate') -%}
  <li>{{ post.data.lastEditDate }} - <a href="{{ post.url }}">{{ post.data.title }}</a></li>
{%- endfor -%}
</ul>

<h3>Everything</h3>
{# maybe once I have lots of posts this could be split into categories? #}
{# and can use https://mozilla.github.io/nunjucks/templating.html#groupby ? #}
{# TODO: I should also use tags for categories - posts, notes, projects, etc. #}

<p>TBD</p>

{# select only things that are completed #}
{# TODO: sort by date, descending #}
<ul>
{%- for post in collections.all | rejectattr("data", "inProgress") -%}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
{%- endfor -%}
</ul>

</section>
